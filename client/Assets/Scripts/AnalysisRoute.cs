// Set by a screen (e.g. the Puzzles "Analyze" button) before loading the Analysis scene, so the
// analysis board opens on a specific position instead of the start. Mirrors ThemeRoute/DailyRoute.
public static class AnalysisRoute
{
    public static string Fen;
}
