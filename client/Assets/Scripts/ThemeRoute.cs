// Set by a screen (e.g. Stats "practice weakest") before loading the training scene, to drill a
// single pattern instead of the adaptive loop. Mirrors DailyRoute/EndgameRoute.
public static class ThemeRoute
{
    public static string PatternId;
    public static string PatternName;
}
