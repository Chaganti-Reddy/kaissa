// Set by the menu before loading the training scene to request the daily puzzle instead of the
// normal adaptive training loop. Mirrors EndgameRoute.
public static class DailyRoute
{
    public static bool Active;
}
