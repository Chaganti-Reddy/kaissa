# Claude working area (private, not part of the public repo)
.claude/

# Build output
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Rr]elease/
artifacts/

# .NET / Rider / VS
*.user
.vs/
.idea/
*.suo

# Test results
[Tt]est[Rr]esults/
*.trx
coverage*.json
coverage*.xml

# Downloaded engine binaries (fetched via scripts/fetch-stockfish.ps1, not committed)
third_party/stockfish/

# Raw Lichess puzzle database (large, CC0; drop here and run the importer). Not committed.
third_party/lichess/
*.csv.zst
lichess_db_puzzle.csv
/*.csv

# Importer output before it is curated into Content/. Not committed.
generated-scenarios.json

# Local training progress and game-derived practice written by the CLI
kaissa-progress.json
kaissa-practice.json

# OS
.DS_Store
Thumbs.db

# Unity (added later when the client project lands)
[Ll]ibrary/
[Tt]emp/
[Ll]ogs/
[Uu]serSettings/
*.csproj.meta

# Player build output + installer artifacts (produced by release.yml, or local verify builds)
/build/
/build-verify/
/installer-out/
/installer/installer-out/
unity-build.log
verify-*.log
verify-*.png
preview.log
piece-preview.png
Kaissa-Windows-*.zip

# screenshot-harness output (local test artifacts)
/shots/
/shots2d/
/shotsI/
/shots3d/
/rec/
/pt/
/pt3d/
/shotsANN/
/shotsPZ/
/shotsRush/
/shotsVerify/
